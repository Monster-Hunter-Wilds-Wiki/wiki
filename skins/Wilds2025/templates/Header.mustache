<header id="mw-header">
  <div id="header-logo-container" class="mw-portlet" role="banner">
    <a href="{{link-mainpage}}" id="header-logo">
      {{#data-logos}}
        {{#icon}}<img src="{{.}}" width="40" height="40">{{/icon}}
        {{#wordmark}}<img src="{{src}}" width="{{width}}" height="{{height}}">{{/wordmark}}
        {{^wordmark}}<h1 id="header-logo-title">{{msg-sitetitle}}</h1>{{/wordmark}}
      {{/data-logos}}
    </a>
  </div>
  {{#data-search-box}}
    <div id="header-search">
      <form action="{{form-action}}" role="search" class="mw-portlet" id="p-search">
        <input type="hidden" name="title" value="{{page-title}}">
        <h3>
          <label for="searchInput">{{msg-search}}</label>
        </h3>
        {{{html-input}}}
        <input type="submit" name="go" value="Go" title="Go to a page with this exact name if it exists" class="searchButton" id="searchButton">
        <span class="material-icons search-icon">search</span>
      </form>
    </div>
  {{/data-search-box}}
  {{>User}}
</header>